version: '3.8'
services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  dashboard:
    build:
      context: ./dashboard
    ports:
      - "8080:80"
    depends_on:
      - backend
